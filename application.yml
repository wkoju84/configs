eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
#  endpoint:
#    health:
#      show-details: always
info:
  author: William Silva
  mail: wkoju84@gmail.com
  cel: 000000000
server:
  port: ${PORT:9999}
message: Mensagem generica